# Harden Run — 2026-07-25 (remediation batch 6, wave3 start)

Queue positions 26-28 plus the two batch-1 reverts requeued with their exact residual defects.

## Revised (5/5, verified clean)
- FEE-1615 knip (REQUEUE): the sole batch-1 blocker (wrong catalog-rule gloss) fixed and verified
  against knip.dev (pnpm/Yarn/Bun workspace catalog entries); also corrected a --fix file-deletion
  error, added the cycles rule, added the two independent Vanderkam references. Clean on second attempt.
- FEE-12003 Speculation Rules (REQUEUE): all three batch-1 blockers fixed — invented DevTools panel
  removed, the mechanically-impossible client-side Sec-Purpose claim corrected (it is a request header
  the browser sends to the server), wrong Shopify year fixed; invented hit-rate thresholds cut.
- FEE-508 Micro-Frontends: fabricated adoption thresholds ("fewer than 3 teams / 20 routes") removed;
  Module Federation landscape verified.
- FEE-11203 CSS @layer: 19 dash sites cleared; @layer support verified.
- FEE-1506 CI Secrets: leaked-key-abuse timing and SLSA levels sourced; OIDC keyless-auth guidance
  updated; corpus-high 7 tricolons and 16 dash sites cleared.

## Note
Both requeued reverts landed clean this run, validating the requeue-with-exact-residuals mechanism:
a reverted article's specific surviving defects become the next run's operator note.
