# Harden Run — 2026-07-24 (remediation batch 1 of tier 1)

Targets: top 5 of the triage remediation queue, each with its triage evidence as an operator note.

## Revised
- FEE-207 CSS Custom Properties & Theming — all 10 unsourced claims verified or anchored
  (@property Baseline July 2024 confirmed against web.dev; Open Props size corrected to ~4KB);
  preprocessor history now states coexistence; Chakra v3 syntax and a real rAF batching bug fixed;
  light-dark(), color-scheme, and container style queries added with verified Baseline dates;
  20+ dash sites cleared; slug added. Verified clean.
- FEE-205 CSS Architecture & Scoping — the FEE-607-twin "pendulum pattern" era narration replaced
  with coexisting approaches and partial adoption; @layer/nesting/@scope dates verified;
  26 dash sites and 6 tricolons cleared. Verified clean.
- FEE-203 Flexbox & Grid — "CSS Grid made all of these obsolete" corrected to honest coexistence;
  spec milestones verified; 21 dash sites cleared. Verified clean.

All three zh-TW mirrors fully synced.

## Reverted (blocking factual defects survived 3 verify rounds; requeued)
- FEE-1615 knip — the revision was otherwise strong (all predecessor-archive claims verified with
  verbatim README quotes; invented persona removed) but a wrong gloss of the catalog rule survived.
- FEE-12003 Speculation Rules — three blocking defects remained: invented DevTools panel details,
  a Sec-Purpose header claim that is mechanically impossible client-side, wrong Shopify reference year.

Both keep ledger entries; next attempt carries the exact residual defects as the operator note.

## Run notes
- 36 agents, 0 errors, ~2.6M subagent tokens, ~46 min. Build gate passed (279s).
- The severity-aware verify policy distinguished correctly this batch: the two reverts were for
  genuine factual defects, not cosmetics.


## Run 2 (remediation batch 2)

Targets: queue positions 6-10. Workflow returned 1 revised + 4 reverted, but every
revert carried only 1-2 one-line-fixable blocking residuals surfaced in the final
round; the operator applied the verifier-prescribed fixes, two focused verifiers
confirmed all four clean, and all five landed. zh mirrors synced (the sync agents
were killed by a session limit after writing but before reporting; their output
passed structural parity, leftover-claim, and dash checks, plus one write-artifact strip).

### Revised (5/5)
- FEE-1501 GitHub Actions: invented runner benchmarks replaced with GitHub-documented
  specs (4vCPU/16GB public, 2vCPU/8GB private); GitLab CI timeline corrected (7.12,
  June 2015); GitHub-centric succession narrative fixed; CVE-2025-30066 SHA-pinning added.
- FEE-1101 Vitest/Jest: invented 8-18s/5-10x benchmarks replaced with sourced Jest 30
  numbers (37%/77%, Jest blog) and npm download data; coexistence stated; Vitest 4 updated.
- FEE-1102 RTL: Enzyme fate honest (last release 2019-12, no official React 18 adapter,
  ~1.3M weekly downloads mid-2026); jest-dom import paths fixed; Storybook 8.3/9 attribution.
- FEE-1509 Rollback: all 12 invented operational numbers sourced, reframed as explicit
  hypotheticals, or cut; rollback-reach section added (open tabs, service workers).
- FEE-1616 Renovate: invented team persona removed; update-lockfile semantics and
  Dependabot platform support aligned with cited sources; Nesbitt seven-day figure quoted.

### Policy change shipped mid-wave (main 253e89a)
Verify loop raised to 5 rounds with adjudication continuity (prior rounds' findings
passed to later verifiers) after this batch showed 3-round caps discarding near-clean
revisions and verifier oscillation on FEE-1616.
