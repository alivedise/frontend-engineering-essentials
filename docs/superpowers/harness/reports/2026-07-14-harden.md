# Harden Run — 2026-07-14

## Revised
(none)

## Clean
(none)

## Reverted / Failed (needs manual attention)
- FEE-1001 docs/en/Accessibility/1001.md — findings {tone: 7, organization: 3, references: 3, factual: 7, template: 4}.
  Revision quality was high per the verifier (tone tricolon removed, AT introduced at first use,
  role=status implicit aria-atomic corrected, first-rule quote reattributed to the W3C Using ARIA Note,
  NSAccessibility name fixed, reference dates verified). Round-1 verify findings (two reference
  attribution errors) were fixed. Round-2 verify surfaced two NEW findings around the added
  redundant-aria-live guidance: (a) the MAY bullet over-generalizes to role="alert" where MDN warns of
  VoiceOver double-speak, (b) "kept for compatibility with older screen readers that predate the
  implicit mappings" is invented specificity. Bounded 2-round policy => full revert.

## zh desync (needs manual attention)
(none — sync stage never ran)

## Run notes
- Smoke run (batch=1), 5 agents, 0 errors, ~310k subagent tokens, ~19 min.
- First selection picked FEE-1000 (overview); owner stopped the run — overview pages are now
  permanently excluded from selection (commit bd3d89d).
- Build gate skipped: no content changes remained after revert.
- Policy question for calibration: round-2-discovered findings with concrete fixes triggered a
  full revert of an otherwise-verified revision. Options: allow a 3rd round when round 2's
  findings are NEW (not round-1 regressions), or partial-revert semantics.


## Run 2

Rerun of FEE-1001 under the autonomous PR-landing model (three-round verify
loop, branch + PR).

### Revised
- FEE-1001 docs/en/Accessibility/1001.md — findings {tone: 5, organization: 3, references: 2, factual: 8, template: 3}.
  All 21 EN-scope findings applied and adversarially verified clean (every added claim
  confirmed against ARIA in HTML / WAI-ARIA 1.2 / MDN / Using ARIA primary sources).
  Notable fixes: select implicit role corrected to combobox; role="status" implies
  aria-live="polite" AND aria-atomic="true" (previous text had it backwards); first-rule
  quote reattributed to the W3C Using ARIA note; AXObject replaced with NSAccessibility;
  invented "5%" statistic removed; negation tricolon and three other AI-tone patterns
  replaced with concrete examples; template realigned to canonical order with slug
  frontmatter. zh-TW mirror synced including the two zh-only tone findings.

### Run notes
- 8 agents, 0 errors, ~527k subagent tokens, ~31 min. Verify took 2 rounds
  (round-1 findings fixed, round-2 clean) — the round-2-discovers-new-issues
  case from Run 1 did not recur, but the 3-round headroom now exists.
- Landed via branch harness/harden-2026-07-14 and PR (autonomous landing model).
