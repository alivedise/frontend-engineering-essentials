# Harden Run — 2026-07-17

## Revised
- FEE-607 State Management Libraries — findings {tone: 4, organization: 4, references: 1, factual: 6, template: 1}.
  Recoil noted as archived (2025-01-01) at every mention; new "2024+ Signals convergence" timeline
  entry cross-referencing FEE-611; bundle-size table rebuilt with bundlephobia-verified,
  version-anchored numbers (all seven cells were wrong or unanchored); Nanostores size updated to
  the README's current 340-864 bytes; counter examples replaced with one cart example (items +
  derived total + async refresh) across Redux Toolkit / Jotai / Valtio; adoption sentence sourced
  from npm's official download API after the audit-suggested source proved self-contradictory;
  Learning-curve opinion row regrounded as countable "new concepts to learn"; 14 em-dash prose
  sites and 6 negation tricolons removed; slug added, Related Topics normalized.
- FEE-10001 Temporal — findings {tone: 3, organization: 3, references: 4, factual: 5, template: 2}.
  Status block rewritten: Stage 4 reached March 2026 (ES2026); ships stable unflagged in
  Firefox 139+ (2025-05), Chrome/Edge 144+ (2026-01), Node.js 26+ (2026-05); Safari remains
  Technology Preview behind a flag, so the article stays in the proposals range per FEE-10000's
  graduation policy (noted in the status block). Fabricated claim that Luxon's author co-authored
  Temporal removed (champions list verified); replaced with the verified Maggie Johnson-Pint
  lineage. DST example outputs corrected (the old comments reproduced the very bug the scenario
  warns about). Leap-second claim corrected (Temporal clamps :60, does not "handle" it).
  Migration subsection (Date <-> Temporal bridges) and scenario payoff step added.

## Clean / Reverted / zh desync
None. Both zh mirrors fully synced.

## Structural flags for the owner (not fixed in this run)
1. FEE-10100 is Stage 4 / ES2025 and should graduate out of the proposals range per FEE-12000
   policy (file move + renumber + list.md; needs an owner decision).
2. The reviser discovered a sitewide link/rewrite mismatch: VitePress config rewrite
   'en/:path+/:page': ':page' strips locale and category from real URLs, so the repo-wide
   cross-link convention /en/<category>/<id-or-slug> (mandated by CLAUDE.md and used across
   sibling articles) does not match served routes. Kept the repo convention in both revised
   articles for consistency; the mismatch needs a single sitewide decision (fix the rewrite rule
   or the link convention).

## Run notes
- Batch of 2, 16 agents, 0 errors, ~1.26M subagent tokens, ~46 min wall clock (articles pipelined).
- First run with severity-aware verify verdicts and the any-density em-dash rule; both articles
  reached verified-clean without needing the minor-residual escape hatch.
- Build gate passed (219s).
