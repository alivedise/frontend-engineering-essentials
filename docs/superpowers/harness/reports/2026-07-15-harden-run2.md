# Harden Run — 2026-07-15 (Run 2: FEE-10300)

## Revised (operator override of a bounded-loop revert)
- FEE-10300 docs/en/Web Platform Proposals/TC39 and JS Proposals/10300.md — findings
  {tone: 3, organization: 3, references: 5, factual: 8, template: 3}.
  The pre-revision article carried blocker-class hallucinations: "Shipped Chrome 130+,
  Firefox 133+" (decorators have NEVER shipped in any browser; caniuse 0%), current
  status stated as Stage 3 (demoted to Stage 2.7 on 2026-05-19 — browsers unwilling
  to ship, insufficient tests; Decorator Metadata demoted in the same session), and
  claims that Angular 18/NestJS 10 migrated to TC39 decorators (both still require
  experimentalDecorators; Angular issue #56146 was closed by its author in 13 minutes).
  The revision corrected all of these, rebuilt the addInitializer timing table to match
  the proposal README, fixed the Symbol.metadata section (no engine defines it),
  updated the transpiler/runtime matrix (TS 5.0/5.2, Babel, SWC, esbuild 0.21.0,
  Bun 1.3.10, Node type-stripping), and removed a dead FEE-10302 cross-link.
  First run using the per-article operator note (category conventions protected;
  status claims flagged as suspected hallucinations — confirmed).

## Policy note
The workflow returned status "reverted": after 3 verify rounds a single COSMETIC tone
finding (one negation tricolon introduced by the revision) still survived, and the
bounded loop condemns on any non-clean verdict. The verifier's own notes called the
revision "factually sound and substantially improved". The operator applied the
verifier-prescribed one-sentence fix and landed the revision — the PR remains the
human gate. Follow-up: verify findings need a severity field so minor-only residuals
pass with notes instead of triggering a whole-article revert.

## Run notes
- 7 agents, ~575k subagent tokens, ~45 min (the 545-line article and the operator-note
  verification mandate made this the heaviest run so far).
- Build gate passed (241s). zh mirror fully synced (28 headings, 1:1 parallel).
