---
id: 803
title: "轉譯：Babel、TypeScript 編譯器與 SWC"
state: draft
category: Build Tooling and Module Systems
---

# 轉譯：Babel、TypeScript 編譯器與 SWC

## 背景

2014 年之前，撰寫現代 JavaScript 意味著等待瀏覽器跟上腳步。ECMAScript 2015（ES6）引入了
類別、箭頭函式、解構賦值與模組——但各家瀏覽器的支援時程不一，Internet Explorer 更是落後
數年。現實的選擇只有兩個：繼續停留在 ES5，或接受有相當比例的用戶看到破損的頁面。

Babel 徹底改變了這個限制。透過將現代 JavaScript 編譯成舊版語法，它解開了「你能寫什麼」
和「瀏覽器能執行什麼」之間的耦合。開發者開始在規格定稿當天就採用 ES2015+ 特性，而不是
等待兩到四年讓瀏覽器廣泛支援。「現在就用」成為前端開發的運作模式。

轉譯（transpilation）是原始碼對原始碼的編譯：輸入與輸出都是人類可讀的原始碼，只是方言或
功能集不同。這有別於編譯成機器碼（Rust、C++ 編譯器的工作）或位元組碼（JVM、.NET CLR 所
消費的格式）。現代前端專案中主要的轉譯工作包括：

- **語法降階（Syntax lowering）** — 將 `async/await`、可選鏈結、空值合併運算子、類別欄位
  轉換成舊版執行環境能理解的 ES5 或 ES2015 語法。
- **型別剝離（Type stripping）** — 移除 TypeScript 型別標注與 JSX 標記，使輸出成為
  瀏覽器或 Node.js 可執行的合法 JavaScript。
- **JSX 轉換** — 將 `<Component />` 轉換為 `React.createElement(Component, null)` 或等效
  的自動 runtime 呼叫。

這些工作外表相似，但效能特徵截然不同。型別剝離本質上是文字轉換：一旦找到標注的邊界，
刪除操作就很直接。型別**檢查**——驗證 `user.id` 在你承諾的地方確實是 `string`——則需要
理解整個程式：所有 import、所有推斷出的型別、所有多載的函式簽章。「剝離」與「檢查」之間
的差異，驅動了現代建置流程的整體架構。

## 設計思維

**Babel 為何具有變革性。**
Babel 帶來的真正洞見不在技術層面，而在時間層面。Babel 出現之前，JavaScript 社群以落差
（lag）為常態運作：功能被提案、標準化、由瀏覽器廠商實作，然後才被應用程式碼採用。這段落差
以年計算。Babel 將它壓縮成零。當 TC39 發布已定稿的提案，Babel 外掛在數天內便出現。團隊在
所有瀏覽器原生支援之前，就開始在正式環境使用可選鏈結與空值合併運算子。這改變了生態系的步調：
特性不再從規格慢慢流向程式碼，而是平行推進。這意味著後來的所有轉譯器（SWC、esbuild、tsc）
都存在於 Babel 所創造的世界——一個將編譯步驟視為開發第一公民的世界。

**TypeScript 為何贏得型別安全之爭。**
TypeScript 並不是第一個嘗試為 JavaScript 加入靜態型別的工具——Closure Compiler 與 Flow
都更早出現。TypeScript 勝出有三個結構性原因。第一，它的型別系統是**結構化（structural）**
而非名義化（nominal）的：若兩個型別具有相同的形狀，它們就相容。這讓採用得以漸進——接受
`{ name: string }` 的函式，對任何擁有 `name` 字串屬性的物件都能生效，無需明確宣告意圖。
第二，TypeScript 允許增量採用：一個 `.js` 檔案改名為 `.ts` 而不做其他修改，就是合法的
TypeScript。團隊可以逐檔遷移，歷時數月，而不必進行全面重寫。第三，生態系對齊了：
DefinitelyTyped 為數千個 npm 套件提供型別定義，主要框架（React、Vue、Angular）也發布了
第一方型別。到 2023 年，TypeScript 已成為任何這些框架新專案的預設起點。

**SWC 與 esbuild 為何在轉譯速度上取代 Babel。**
Babel 的瓶頸在於 JavaScript 執行環境。將一個檔案解析成 AST、透過外掛鏈遍歷該 AST，再將
修改後的 AST 節點印回原始碼——這一切都在 V8 中執行。V8 的通用運算能力很快，但 AST 操作
是配置（allocation）密集的：每個節點都是一個物件，每次轉換都建立新物件，垃圾回收器（GC）
會因此暫停。SWC 用 Rust 完成相同的工作，Rust 編譯成原生碼，沒有 GC 暫停，且能在多條 OS
執行緒上同時處理多個檔案。結果是：SWC 在單執行緒上比 Babel 快約 20 倍，跨多核心最高可達
70 倍。

esbuild 以 Go 採取相同的原生碼方案，特別注重平行處理。兩者都刻意省略型別檢查——這不是
限制，而是設計選擇。型別檢查需要全程式分析；轉譯是逐檔且高度可平行化的。將兩者分離，可在
熱路徑（開發時每次存檔）中獲得最快的轉換，同時在 CI 中只執行一次昂貴的檢查。

**`tsc --noEmit` + SWC 的模式。**
架構洞見在於：型別檢查與轉譯是效能特徵截然不同的兩個獨立關注點。型別檢查天生就慢——它需要
解析每個 import、推斷每個泛型、遍歷整個依賴圖。再聰明的實作都無法改變這一點；這是型別
檢查本身的性質決定的。轉譯天生就快——它是逐檔、可平行、無需跨檔知識的。將兩者解耦，可以
讓每個關注點都交給為它最佳化的工具：

- 在開發環境：SWC 依需求轉換檔案，帶來 100 毫秒以下的 HMR 更新。
- 在 CI：`tsc --noEmit` 對整個專案執行一次，回報型別錯誤。若通過，SWC 產生可發布的輸出。

Next.js 12（2021 年）是第一個將這個分離模式設為預設的主要框架。從 Next.js 11 升級到 12
不需要任何程式碼變更；框架悄悄將所有轉換從 Babel 切換到 SWC，達成 17 倍更快的快速刷新
與 5 倍更快的建置。若存在 `.babelrc` 檔案，Next.js 會自動退回使用 Babel，為有自訂 Babel
外掛的專案保留向後相容性。

**`moduleResolution: "bundler"` 為何存在。**
TypeScript 的 `node` 解析模式是為 Node.js 中 `require()` 的運作方式設計的：副檔名可選、
嘗試 `index.js` 退回選項、以 `package.json` 的 `main` 作為進入點。這個模型與 Vite 和
Webpack 等打包工具實際解析模組的方式有所偏差：它們遵守 `package.json` 的 `exports` 欄位
（`node` 解析忽略此欄位）、允許 ESM 中無副檔名的 import，且不要求相對路徑附帶 `.js`
後綴。在 Vite 專案中使用 `moduleResolution: "node16"` 或 `"node"` 會讓 TypeScript 對合法
的打包工具解析的 import 發出錯誤的型別錯誤。TypeScript 5.0 引入了 `moduleResolution: "bundler"`，
精確地模擬打包工具語意：查閱 `exports` 欄位、不要求副檔名，並優先選擇 `import` 條件而非
`require` 條件。

## 最佳實踐

**在 CI 中應將型別檢查（`tsc --noEmit`）與轉譯（SWC 或 esbuild）分離，並將型別檢查設為必要的 CI 步驟。**
型別剝離是逐檔且可平行的；型別檢查則需要全程式分析，天生就比較慢。以 SWC 負責轉換、`tsc --noEmit` 作為阻塞式 CI 關卡，能在開發時提供最快的熱路徑，同時確保型別錯誤在合併前被捕捉。對 Vite 專案而言，`pnpm tsc --noEmit && pnpm vite build` 是標準模式。

**所有使用 `@babel/preset-env` 的專案，都應明確設定 browserslist 目標。**
沒有 `browserslist` 設定，`@babel/preset-env` 就沒有目標瀏覽器的資訊，可能套用所有轉換（產生不必要的大型輸出），也可能一個都不套用。請在 `package.json`（`"browserslist"` 欄位）或 `.browserslistrc` 檔案中定義目標，並隨著你的支援範圍演進而持續更新。

**以 Vite、Webpack 或其他打包工具建置的專案，`tsconfig.json` 中應使用 `moduleResolution: "bundler"`（TypeScript 5.0+）。**
`node` 解析策略不理解 `package.json` 的 `exports` 欄位，而現代雙格式套件（dual-format package）用此欄位選擇 CJS 或 ESM。使用錯誤的解析模式，會導致 TypeScript 對合法的打包工具解析 import 發出錯誤的型別錯誤。當 tsc 僅用於型別檢查時，應搭配 `"module": "ESNext"` 和 `"noEmit": true`。

**絕對不能將 `@babel/preset-typescript` 視為型別檢查工具。**
Babel 剝離 TypeScript 標注時不驗證它們。一個含有錯誤型別標注的檔案仍會成功轉譯，不產生任何錯誤。型別錯誤只有在 `tsc --noEmit` 對整個專案執行時才會浮現。使用 Babel 而沒有獨立的 tsc 步驟，對型別安全毫無保障。

**路徑別名必須在 `tsconfig.json` 和打包工具中分別設定——它們是各自獨立的系統。**
`tsconfig.json` 中的 `paths` 能滿足 TypeScript 進行型別檢查的模組解析需求，但 SWC、esbuild 和 Vite 各自使用獨立的別名設定。只在 tsconfig 中定義 `paths` 對打包工具的執行時期模組解析毫無影響。必須在 Vite 中設定 `resolve.alias`（或在你的打包工具中設定等效項），以匹配 `tsconfig.json` 中的每一個別名。

## 視覺化

現代專案中 TypeScript 原始碼的雙路徑流程：

```
TypeScript 原始碼（.ts / .tsx）
         │
         ├─────────────────────────────────────────────────┐
         │                                                 │
         ▼                                                 ▼
  tsc --noEmit                                    SWC / esbuild
  （全程式分析）                                  （逐檔，可平行）
         │                                                 │
         ▼                                                 ▼
  型別錯誤                                         JS 輸出（.js）
  （在 CI 中回報）                                 （無型別，無 JSX）
  通過 / 失敗                                             │
                                                          ▼
                                                    打包工具（Rollup /
                                                    Webpack / esbuild）
                                                          │
                                                          ▼
                                                   正式環境打包結果
```

左路徑在每次 CI 任務執行一次。右路徑在開發環境中於每次存檔時執行。

## 範例

### 1. 含 browserslist 的 Babel 設定

`.babelrc.json`：

```json
{
  "presets": [
    [
      "@babel/preset-env",
      {
        "useBuiltIns": "usage",
        "corejs": "3.37"
      }
    ],
    ["@babel/preset-react", { "runtime": "automatic" }],
    "@babel/preset-typescript"
  ]
}
```

`package.json`（browserslist 欄位，由 `@babel/preset-env` 自動讀取）：

```json
{
  "browserslist": ["> 0.5%", "last 2 versions", "not dead"]
}
```

`@babel/preset-env` 會查詢 browserslist 資料庫，判斷要套用哪些轉換。若沒有 `browserslist`
設定，它就沒有目標資訊，預設可能轉換所有內容——產生比必要更大、更慢的輸出。

`@babel/preset-typescript` **剝離** TypeScript 標注；它**不**做型別檢查。需要另外執行
`tsc --noEmit` 才能捕捉型別錯誤。

### 2. 含注解的 `tsconfig.json` 關鍵選項

```json
{
  "compilerOptions": {
    // 輸出語法版本。"ES2022" 對 Node 18+ 與現代瀏覽器是安全的選擇。
    // 當 SWC 或 esbuild 負責 emit 時，tsc 仍會讀取此欄位進行型別推斷。
    "target": "ES2022",

    // "bundler"（TypeScript 5.0+）：模擬 Vite/Webpack 解析模組的方式。
    // 遵守 package.json "exports"，允許相對 import 不附副檔名。
    // 僅對無打包工具的 Node.js CLI/函式庫使用 "node16"。
    "moduleResolution": "bundler",

    // 使用 moduleResolution "bundler" 時的必要設定。
    "module": "ESNext",

    // 啟用：strictNullChecks、noImplicitAny、strictFunctionTypes 等。
    // 新專案從 true 開始；在既有程式碼上加入需要修復錯誤。
    "strict": true,

    // 路徑別名——也必須在打包工具中配置相同的別名。
    "paths": {
      "@/*": ["./src/*"]
    },

    // 不寫出 .js 檔案；tsc 僅用於型別檢查。
    // 若 tsc 是你的 emit 步驟（在打包工具專案中很少見），則移除此旗標。
    "noEmit": true,

    // 在型別檢查中包含 .tsx 檔案。
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
```

### 3. Next.js：SWC 編譯器設定

`next.config.mjs`（Next.js 13+；SWC 已是預設，無需額外旗標）：

```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // SWC 自 Next.js 12 起即為預設編譯器。
  // 以下選項設定 SWC 特有的轉換。

  compiler: {
    // 從正式環境建置中移除 console.log 呼叫。
    removeConsole: process.env.NODE_ENV === "production",

    // 啟用 styled-components 支援（取代 babel-plugin-styled-components）。
    styledComponents: true,
  },

  // 若存在 .babelrc，Next.js 會自動退回使用 Babel。
  // 無法強制在有 .babelrc 的情況下使用 SWC——請刪除 .babelrc。
};

export default nextConfig;
```

從 Babel 遷移到 Next.js 中的 SWC：刪除 `.babelrc`（或 `.babelrc.json`），並從
`devDependencies` 移除 `babel-loader`。下次執行 `next dev` 或 `next build` 時，Next.js
就會使用 SWC。若有自訂 Babel 外掛且找不到對應的 SWC 版本，保留 `.babelrc` 檔案即可——
Next.js 會逐檔退回使用 Babel。

### 4. CI 指令：型別檢查後再轉譯

```bash
# 在 CI 的建置前執行。有任何型別錯誤即以非零值退出。
pnpm tsc --noEmit

# 若 tsc 通過，使用 SWC 輸出可發布的結果。
# -d 設定輸出目錄；--source-maps 包含 source map。
pnpm swc src -d dist --source-maps

# 或在 Next.js 專案中，讓框架統籌兩者：
pnpm tsc --noEmit && pnpm next build
```

在 Vite 專案中，`vite build` 內部使用 esbuild 剝離型別。需將 `tsc --noEmit` 作為獨立步驟
加在建置之前或之後，而非取代它：

```bash
pnpm tsc --noEmit && pnpm vite build
```

### 5. 透過 `@swc/core` 以程式方式使用 SWC

```js
import { transform } from "@swc/core";

const result = await transform(
  `const greet = (name: string) => \`Hello, \${name}\`;`,
  {
    jsc: {
      parser: { syntax: "typescript" },
      target: "es2020",
    },
    sourceMaps: true,
  }
);

console.log(result.code);
// const greet = (name) => `Hello, ${name}`;
```

## 常見錯誤

**將 `@babel/preset-typescript` 當作型別檢查工具。**
Babel 剝離型別時不驗證它們。一個充滿錯誤型別標注的檔案仍會成功轉譯。務必在 CI 中執行
`tsc --noEmit`——Babel 對型別正確性不提供任何保證。

**省略 browserslist 目標。**
沒有 browserslist 設定，`@babel/preset-env` 就沒有目標瀏覽器的資訊。它可能套用所有轉換
（使輸出比必要更大），或一個都不套用（視版本而定）。請在 `package.json` 或
`.browserslistrc` 中明確定義目標。

**在 Vite 或 Webpack 專案中使用 `moduleResolution: "node"`。**
`node` 策略不理解 `package.json` 的 `exports` 欄位。使用 `exports` 進行 CJS/ESM 雙重
發布的套件——包括許多現代函式庫——會被錯誤解析，導致不反映實際執行期行為的型別錯誤。
TypeScript 5.0+ 的打包工具專案請使用 `moduleResolution: "bundler"`。

**未將 `tsc --noEmit` 納入 CI。**
在編輯器中進行型別檢查能捕捉單一檔案的錯誤，但 TypeScript 的完整跨檔推斷只有在編譯器看到
整個程式時才會啟動。涉及循環 import、缺少模組擴增或不正確泛型約束的錯誤，往往只有在
`tsc --noEmit` 對整個專案執行時才會浮現。請將其設為必要的 CI 步驟，而非選填。

**在 Next.js 專案中不必要地保留 `.babelrc`。**
一個 `.babelrc` 檔案會讓 Next.js 對所有檔案停用 SWC 並退回 Babel，而非只有需要自訂外掛
的檔案。若只需要一個外掛，在保留 Babel 完整效能代價之前，先評估是否有對應的 SWC 外掛或
Next.js 內建的編譯器選項。

**忘記 SWC 與 esbuild 不執行 TypeScript 的 `paths` 別名。**
`tsconfig.json` 中的 `paths` 是 TypeScript 的概念。SWC、esbuild 和 Vite 各有自己的別名
設定（`jsc.paths`、esbuild 的 `alias`、Vite 的 `resolve.alias`）。在 `tsconfig.json` 中
有 `paths` 可以滿足型別檢查器的要求，但對打包工具的模組解析毫無影響——除非你也在打包工具
中做了對應設定。

## 相關 FEE

- **FEE-800** — 建置工具總覽：本文所屬的大型建置工具格局。
- **FEE-801** — 打包工具（Webpack、Rollup、esbuild）：打包工具如何在內部調用轉譯器。
- **FEE-307** — ES 模組：`moduleResolution: "bundler"` 為之設計的模組格式。
- **FEE-804** — `package.json` 欄位：`exports`、`main`、`module`——`moduleResolution: "bundler"` 所查閱的欄位。

## 參考資料

- [SWC 文件](https://swc.rs/docs/getting-started)
- [SWC 與 Babel 效能比較](https://swc.rs/blog/perf-swc-vs-babel)
- [Babel @babel/preset-env](https://babeljs.io/docs/babel-preset-env)
- [Babel @babel/preset-typescript](https://babeljs.io/docs/babel-preset-typescript)
- [TypeScript tsconfig 參考](https://www.typescriptlang.org/tsconfig)
- [TypeScript 5.0 — 宣布 moduleResolution: bundler](https://devblogs.microsoft.com/typescript/announcing-typescript-5-0/)
- [TypeScript 模組解析指南](https://www.typescriptlang.org/docs/handbook/modules/guides/choosing-compiler-options.html)
- [Next.js 編譯器架構](https://nextjs.org/docs/architecture/nextjs-compiler)
- [Next.js 12 發布 — SWC 預設編譯器](https://nextjs.org/blog/next-12)
- [browserslist](https://github.com/browserslist/browserslist)
