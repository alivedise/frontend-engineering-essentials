---
id: 11100
title: "Scroll-driven Animations — 捲動驅動動畫"
state: draft
slug: scroll-driven-animations
level: senior
reviewed: hardened
reviewed_on: 2026-07-25
---

# [FEE-11100] Scroll-driven Animations — 捲動驅動動畫

:::info
依據捲動位置為元素套用動畫效果（進度條、視差、淡入等），完全以 CSS 實現，不需要任何 JavaScript。若瀏覽器讓動畫在主執行緒之外執行，忙碌的主執行緒就不會使動畫停滯；哪些瀏覽器符合這個條件，請參閱下方「為什麼 CSS 原生方案更優」一節。
:::

:::info Baseline：有限支援
Chrome 115+、Edge 115+ 已支援。Safari 於 Safari 26（2025 年 9 月）加入支援；具執行緒化、可在主執行緒外執行的版本則到 Safari 26.4 才推出。Firefox 截至 Firefox 152（2026 年 6 月），穩定版仍需開啟 `layout.css.scroll-driven-animations.enabled` 旗標才能使用；該功能在 Nightly 版本已預設開啟，並被列為 Interop 2026 的優先項目之一。正式使用前請至 MDN 確認最新 Firefox 支援狀態。
:::

## 背景

捲動連動效果一直是現代 UI 的常見需求：文章頂端的閱讀進度條、元素進入視口時的淡入效果、隨頁面捲動改變樣式的固定導覽列。這些效果過去全都需要 JavaScript。

典型的做法是結合 `scroll` 事件監聽器與 `requestAnimationFrame`：

```js
window.addEventListener('scroll', () => {
  requestAnimationFrame(() => {
    const progress = window.scrollY / (document.body.scrollHeight - window.innerHeight);
    progressBar.style.transform = `scaleX(${progress})`;
  });
});
```

這個做法有一個根本缺陷：`scroll` 事件在主執行緒上觸發。如果主執行緒正在執行耗時任務（解析腳本、處理 React 重新渲染、處理大型資料集），捲動處理函式就會被延遲執行。使用者捲動頁面，進度條要等到任務完成才會跟著移動。

`IntersectionObserver` 改善了元素進入或離開視口的偵測（它會偵測元素何時跨越視口邊界），但無法表達基於進度的動畫。元素不是「正在交叉」就是「沒有交叉」，不存在「進入視口 30%」的回呼。

第三方函式庫填補了這個空缺。GSAP ScrollTrigger 和 Locomotive Scroll 提供了成熟可靠的捲動動畫 API。它們透過 `requestAnimationFrame` 迴圈與 `will-change` 提示來降低卡頓，但仍然在主執行緒上執行，並為每個使用的頁面增加額外的 JavaScript 體積。

CSS 捲動驅動動畫取代了這種做法。`animation-timeline` 屬性讓動畫的進度直接來自捲動位置，而不是仰賴 JavaScript 事件，並沿用既有處理 `transform` 和 `opacity` 動畫的關鍵影格系統。這是否能消除主執行緒卡頓，取決於瀏覽器；哪些引擎能在主執行緒之外執行動畫，請參閱〈設計思維〉中的「為什麼 CSS 原生方案更優」一節。

具體來說：一個長篇文章頁面需要一個隨讀者從頁首捲動到頁尾而填滿的閱讀進度條，以及一系列在捲入視口時淡入的卡片。用傳統方式實作，這代表兩個 `scroll` 監聽器與兩個 `requestAnimationFrame` 迴圈，且各自都需要自己的清理邏輯。使用 CSS 捲動驅動動畫，兩種效果都能完全以 CSS 宣告；下方〈範例〉一節會示範這兩種效果。

## 視覺對比

捲動驅動動畫透過兩種機制之一綁定捲動位置：參照最近捲動容器的匿名時間軸，或是任何具備存取權限的元素都能引用的具名時間軸。

| | 匿名時間軸 | 具名時間軸 |
|---|---|---|
| 語法 | `scroll()` / `view()` | `scroll-timeline-name` + `animation-timeline: --name` |
| 捲動容器 | 最近的可捲動祖先（或 `root`） | 任意具名容器；僅限後代元素，除非有共同祖先設定 `timeline-scope` |
| 適用情境 | 單一元素與其祖先的捲動同步 | 協調多個元素（包含非後代元素）與同一捲動區域同步 |
| 程式碼量 | 最少 | 需在容器上宣告，在動畫元素上引用，若元素非後代則還需 `timeline-scope` |

大多數捲動連動效果使用匿名時間軸更簡潔清晰。當動畫元素不是捲動容器的後代，或多個不相關的元素需要與同一捲動區域同步動畫時，才需要具名時間軸。

## 範例

以下兩個範例都包裹在同一個 `@media (prefers-reduced-motion: no-preference)` 區塊中，符合最佳實踐要求。

### 範例一：使用 `scroll()` 的閱讀進度條

```html
<div class="progress-bar"></div>
<article class="content">
  <!-- 長篇文章內容 -->
</article>
```

```css
.progress-bar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 4px;
  background: #e2e8f0;
  z-index: 100;
  transform-origin: left;
}

@media (prefers-reduced-motion: no-preference) {
  @keyframes grow-progress {
    from { transform: scaleX(0); }
    to   { transform: scaleX(1); }
  }

  .progress-bar {
    animation: grow-progress linear;
    animation-timeline: scroll(root block);
    /* root = 文件根捲動容器，block = 垂直軸 */
  }
}
```

`scroll(root block)` 將動畫連結至根捲動容器（整份文件）的 block（垂直）軸。使用者從頁面最頂端捲動到最底端的過程中，`grow-progress` 從 `scaleX(0)` 執行到 `scaleX(1)`，將進度條填滿。

省略引數（也就是單純寫 `scroll()`）會使用最近的可捲動祖先元素和 block 軸。對於全頁進度指示器，務必明確指定 `root`，無論 DOM 巢狀結構如何，都能確保使用正確的捲動容器。

### 範例二：使用 `view()` 的卡片淡入效果

```html
<section class="cards">
  <div class="card">卡片一</div>
  <div class="card">卡片二</div>
  <div class="card">卡片三</div>
</section>
```

```css
.card {
  /* 動畫播放前的基底狀態 */
  opacity: 1;
  transform: translateY(0);
  background: #fff;
  padding: 2rem;
  margin-block-end: 2rem;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgb(0 0 0 / 0.1);
}

@media (prefers-reduced-motion: no-preference) {
  @keyframes fade-up {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .card {
    animation: fade-up ease-out both;
    animation-timeline: view();
    /* 在元素進入階段的前 40% 內播放完整動畫 */
    animation-range: entry 0% entry 40%;
  }
}
```

`view()` 將動畫連結至元素本身在捲動容器中的可見度。`animation-range: entry 0% entry 40%` 告訴瀏覽器：從元素開始進入視口的瞬間，到它進入 40% 的位置，播放完整的 `fade-up` 關鍵影格序列。`animation` 簡寫中的 `both` 填充模式，在進入前保持 `opacity: 0`，在離開後保持 `opacity: 1`。

## 最佳實踐

- MUST 將所有捲動驅動動畫規則包裹在 `@media (prefers-reduced-motion: no-preference)` 中。會移動元素、改變透明度或影響佈局的捲動驅動動畫，可能在敏感使用者身上觸發前庭功能障礙（vestibular disorders）。這個包裹層不可省略。
- SHOULD 優先在捲動驅動的 `@keyframes` 中只對 `transform` 和 `opacity` 設定動畫。這兩個屬性屬於合成器可處理的屬性（compositor-eligible）：不觸發佈局或繪製，可在合成器執行緒上獨立執行。
- MUST NOT 對 `top`、`left`、`width`、`height`、`margin`、`padding` 或任何會觸發佈局重新計算的屬性設定動畫。這樣做會在每次捲動時強迫瀏覽器執行佈局，完全抵消移至 CSS 帶來的效能優勢。
- SHOULD 使用 `animation-timeline: view()` 實現「元素進入視口時播放動畫」的效果。
- SHOULD 使用 `animation-timeline: scroll()` 實現全頁進度指示器（閱讀進度條、視差背景）。
- 若該效果對佈局而非純裝飾至關重要，SHOULD 搭配 `@supports (animation-timeline: view())` 進行漸進增強。
- 使用 `view()` 時間軸時，SHOULD 設定 `animation-fill-mode: both`，以保留元素離開視口後的最終關鍵影格狀態。

## 設計思維

### 為什麼 JS 捲動監聽器會造成卡頓

JavaScript 事件處理函式，包括 `scroll`，在瀏覽器的主執行緒上觸發。主執行緒同時負責解析 HTML 和 JavaScript、執行框架程式碼、處理使用者輸入以及執行計時器。在繁忙的頁面上，主執行緒可能一次被佔用數十甚至數百毫秒。

當 `scroll` 事件觸發時，若主執行緒正在忙碌，事件會排隊等待。等到處理函式真正執行時，使用者已經捲動得更遠，動畫只能跳躍式地追趕。`requestAnimationFrame` 透過將更新批次到下一次繪製前來減少這種情況，但仍然需要等待主執行緒空閒。

### GSAP ScrollTrigger 如何解決這個問題

GSAP ScrollTrigger 將捲動監聽器包裹在 `requestAnimationFrame` 迴圈中，並對即將產生動畫的元素添加 `will-change: transform` 提示。這告訴瀏覽器將這些元素提升到獨立的合成器層，讓 GPU 處理其 `transform` 變換。效果比原始的捲動監聽器流暢得多，但決定當前進度並套用 CSS 的觸發邏輯，仍然在主執行緒上執行。

### 為什麼 CSS 原生方案更優

CSS 捲動驅動動畫在捲動過程中不會執行 JavaScript。在 Chrome、Edge 以及 Safari 26.4 以上版本中，瀏覽器會在解析階段就登記捲動容器與動畫之間的關係。捲動時，合成器執行緒會直接讀取捲動位置並推進動畫，不會通知主執行緒。合成器執行緒本來就負責處理所有合成 `transform` 和 `opacity` 更新。

在這些引擎上：

- 一個阻塞主執行緒 200 毫秒的 JavaScript 任務不會使動畫停滯；在整個阻塞期間，動畫仍會以正確的捲動位置持續推進。
- 你不需要撰寫任何 JavaScript，因此沒有需要在元件卸載時拆解的 `requestAnimationFrame` 迴圈，也沒有任何監聽器會洩漏。
- 此功能會靜默降級：不支援 `animation-timeline` 的瀏覽器會直接以元素的預設狀態顯示，彷彿從未套用過 `@keyframes`。

Safari 於 Safari 26（2025 年 9 月）推出捲動驅動動畫，但一直在主執行緒上執行，直到 Safari 26.4 才加入執行緒化執行。在 Safari 26.0–26.3 上，你仍然能享有零 JavaScript 的好處，但忙碌的主執行緒仍可能像對待 JavaScript 捲動監聽器一樣，使動畫停滯。只有 Chrome、Edge 以及 Safari 26.4 以上版本，才能同時提供零 JS 撰寫方式與主執行緒外的流暢執行。

## 深入探討

### 具名捲動時間軸

匿名的 `scroll()` 和 `view()` 函式適用於動畫元素是捲動容器後代的情況。當元素不是捲動容器的後代時（例如，一個需要與特定捲動區域同步的固定側邊欄），請搭配 `timeline-scope` 使用具名時間軸：

```css
/* 在 .scroll-region 和 .sidebar-indicator 的共同祖先元素上設定 */
body {
  timeline-scope: --region-timeline;
}

/* 在捲動容器上宣告 */
.scroll-region {
  scroll-timeline-name: --region-timeline;
  scroll-timeline-axis: block;
  /* 簡寫：scroll-timeline: --region-timeline block; */
  overflow-y: scroll;
}

/* 在想要與該容器捲動同步、但非其後代的元素上宣告 */
.sidebar-indicator {
  animation: track-scroll linear both;
  animation-timeline: --region-timeline;
}

@keyframes track-scroll {
  from { transform: scaleY(0); }
  to   { transform: scaleY(1); }
}
```

`scroll-timeline-name` 屬性會將容器宣告為具名捲動時間軸來源，但預設情況下，這個名稱只對宣告元素的後代可見。上面的 `.sidebar-indicator` 並未巢狀於 `.scroll-region` 內，因此若沒有 `timeline-scope`，它的 `animation-timeline: --region-timeline` 參照就會解析不到任何東西，元素也就不會產生動畫。在 `.scroll-region` 和 `.sidebar-indicator` 的共同祖先 `body` 上設定 `timeline-scope: --region-timeline`，會將這個名稱的可見範圍延伸到該祖先元素及其內部的所有元素，這正是讓參照生效的關鍵。

### `@scroll-timeline` 規則集（歷史背景）

規範早期草案中使用 `@scroll-timeline` 規則集來宣告具名時間軸。這個語法出現在非常早期的 Chromium 原型（Chrome 115 之前）和 2021–2022 年的文件中。在功能進入穩定版本之前，已被 `scroll-timeline-name` 和 `scroll-timeline-axis` 屬性所取代。如果你在舊版教學或函式庫中看到 `@scroll-timeline`，請將其視為已過時的語法：任何現代瀏覽器均未實作，也不屬於現行規範。

### `animation-fill-mode: both` 與 view 時間軸

使用 `view()` 時，元素的動畫在捲動時間軸中有明確的起點和終點。元素完全離開視口後，動畫便超出其有效範圍。若未設定 `animation-fill-mode`，元素會還原到初始 CSS 狀態（這可能意味著元素在捲過後又以 `opacity: 0` 重新出現）。設定 `animation-fill-mode: both`（或在 `animation` 簡寫中使用 `both`）可在有效範圍結束後保留最終的關鍵影格數值。

### View 時間軸的各個階段

`animation-range` 屬性接受具名的階段關鍵字，對應元素在捲動容器中移動路徑的各個幾何區域：

| 階段       | 說明 |
|----------|------|
| `entry`  | 元素部分可見且正在進入視口（前緣已越過捲動視口的遠端邊界，後緣尚未越過） |
| `contain`| 元素完全位於捲動視口內，兩端均可見（適用於元素比捲動視口短的情況），或元素完全覆蓋捲動視口，看不到任何一端（適用於元素比捲動視口高的情況，例如高大的主視覺區塊） |
| `exit`   | 元素部分可見且正在離開視口 |
| `cover`  | 完整的可見弧線：從元素前緣首次出現到後緣消失的整段過程 |

混合使用多個階段是合法的：`animation-range: entry 50% exit 50%` 從進入階段的中點開始動畫，在離開階段的中點結束。

### 漸進增強模式

```css
/* 預設：無動畫，元素以最終可見狀態呈現 */
.card {
  opacity: 1;
  transform: none;
}

/* 增強：在支援的瀏覽器中啟用捲動動畫 */
@supports (animation-timeline: view()) {
  @media (prefers-reduced-motion: no-preference) {
    .card {
      animation: fade-up ease-out both;
      animation-timeline: view();
      animation-range: entry 0% entry 40%;
    }
  }
}
```

把完成狀態設為預設樣式，再將捲動動畫放進 `@supports` 中；如此一來，不支援的瀏覽器會顯示完成後的內容、只是沒有動畫，而不是壞掉的內容。這裡將基底狀態設為 `opacity: 1`，代表 Firefox（功能仍在旗標後）或舊版瀏覽器的使用者，都能正常看到所有內容。

### 在同一頁面混用 `scroll()` 與 `view()`

在同一頁面同時使用兩種類型的時間軸不會產生衝突。閱讀進度條可以使用 `animation-timeline: scroll(root block)`，同時各個卡片使用 `animation-timeline: view()`。每個動畫元素各自獨立引用自己的時間軸來源，彼此互不干擾。

對同一個元素套用多個動畫，每個動畫使用不同的時間軸也是合法的，只要 `animation-timeline` 列表中的項目與 `animation-name` 列表中的項目一一對應即可：

```css
.hero-image {
  animation: fade-in ease-out both, parallax-shift linear both;
  animation-timeline: view(), scroll(root block);
  animation-range: entry 0% entry 60%, normal;
}
```

在這個範例中，`fade-in` 使用元素本身的 `view()` 時間軸，而 `parallax-shift` 使用根捲動進度。第二個動畫的 `animation-range: normal` 代表從 0% 到 100% 的完整捲動範圍。

## 瀏覽器支援

| 瀏覽器 | 首次支援版本 | 備註 |
|--------|------------|------|
| Chrome  | 115        | 完整支援：`scroll()`、`view()`、`animation-range`、具名時間軸 |
| Edge    | 115        | 基於 Chromium，與 Chrome 相同 |
| Safari  | 26         | 於 Safari 26（2025 年 9 月）推出。此前在主執行緒上執行，直到 Safari 26.4 加入執行緒化、主執行緒外執行 |
| Firefox | —          | 截至 Firefox 152（2026 年 6 月），穩定版仍需開啟 `layout.css.scroll-driven-animations.enabled` 旗標；Nightly 版本已預設開啟，並為 Interop 2026 的優先項目之一 |

漸進增強檢測：

```css
@supports (animation-timeline: view()) {
  /* 捲動驅動動畫樣式 */
}
```

或在 JavaScript 中進行功能偵測：

```js
const supported = CSS.supports('animation-timeline', 'view()');
```

由於 Firefox 預設未啟用此功能，正式環境使用時應將捲動驅動動畫視為純粹的漸進增強（裝飾性效果，具備優雅的降級），或搭配 `@supports` 守衛與 JavaScript 降級方案，為不支援的瀏覽器提供備援。

## 延伸閱讀

- [FEE-11101 @starting-style](./11101.md)。`@starting-style` 處理元素在首次渲染時的*進入*過渡效果；捲動驅動動畫則處理捲動時間軸上持續進行的進度動畫。兩者結合可讓卡片在初始渲染時播放入場動畫，並在捲動觸發進入時再次播放。
- [FEE-11001 CSS Anchor Positioning](./11001.md)。兩者都是取代 JavaScript 驅動定位與動畫的新一代 CSS 功能：CSS Anchor Positioning 移除了 JS 測量的需求，捲動驅動動畫則移除了 JS 捲動監聽器。

## 參考資料

- [CSS scroll-driven animations — MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_scroll-driven_animations)
- [timeline-scope — MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/CSS/timeline-scope)
- [Animate elements on scroll with Scroll-driven animations — Chrome for Developers](https://developer.chrome.com/docs/css-ui/scroll-driven-animations)
- [A guide to Scroll-driven Animations with just CSS — WebKit Blog](https://webkit.org/blog/17101/a-guide-to-scroll-driven-animations-with-just-css/)
- [WebKit Features for Safari 26.4 — WebKit Blog](https://webkit.org/blog/17862/webkit-features-for-safari-26-4/)
- [Scroll-Driven Animations — Josh W. Comeau](https://www.joshwcomeau.com/animation/scroll-driven-animations/)
- [Scroll-driven Animations 示範 — scroll-driven-animations.style](https://scroll-driven-animations.style/)
- [Interop 2026 — wpt.fyi](https://wpt.fyi/interop-2026)
- [CSS Scroll-Driven Animations Level 1 — W3C Editors' Draft](https://drafts.csswg.org/scroll-animations-1/)
